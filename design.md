# 디자인 아이덴티티 — Japandi Modern & Refreshing Mint

세련된 미니멀리즘 + 정갈한 동양적 여백. 일본식 절제미(Japanese)와 스칸디나비안 미니멀리즘(Scandi)을
섞은 "Japandi" 톤에, 상쾌한 민트/세이지 그린을 더해 학습 앱 특유의 차분한 집중감을 준다.

## 컬러 팔레트

| 역할 | 라이트 | 다크 | 용도 |
|---|---|---|---|
| Primary (딥 세이지 그린) | `#1E5E4B` | `#4FAE8D` | 헤딩, 기본 버튼/테두리, 링크 — 집중과 정적(靜寂)의 마인드풀니스 |
| Primary Light | `#2D7D64` | `#6CC7A5` | 보조 강조, hover 상태 |
| Accent (모던 주홍/테라코타) | `#E06D44` | `#FF8F66` | 정답/성공, 외통(체크메이트) 결과, 핵심 CTA (대국 시작, 다음 단계) |
| Surface — 페이퍼 화이트 | `#F6FAF8` | `#101A17` | 페이지 배경 |
| Surface — 내추럴 버치 | `#F4F1EA` | `#1B2622` | 카드/패널/입력 배경 |
| Text | `#33413C` | `#D7E6E0` | 본문 |
| Text Heading | `#16352C` | `#F1F8F5` | 제목 |
| Border | `#DCE6E1` | `#2B3B36` | 테두리, 구분선 |

라이트/다크 모두 `src/index.css`의 CSS 커스텀 프로퍼티(`--text`, `--text-h`, `--bg`, `--border`,
`--accent`, `--accent-bg`, `--accent-border`, `--primary`, `--primary-light`, `--surface-alt`)로
정의되어 있고, `prefers-color-scheme: dark`를 따라 자동 전환된다.

## 적용 원칙

- **앱 UI 크롬**(배경, 헤더, 버튼, 카드, 상태 메시지)에는 이 팔레트를 전면 적용한다.
- **쇼기 보드 자체(칸/기물)는 전통적인 목재 톤(`#dcb35c` 등)을 그대로 유지**한다. 실제 장기판처럼
  보이는 편이 학습 맥락에 자연스럽고, 기물 가독성에도 유리하기 때문. 따뜻한 원목 색과 세이지
  그린·페이퍼 화이트 배경의 조합 자체가 Japandi 미감에 부합한다.
- Accent 컬러는 아껴서 쓴다: 정답/외통 등 "성공/완료" 신호와, 그 흐름을 이어가는 핵심 액션
  버튼(대국 시작, 다음 단계/레슨 완료)에만 사용한다. 그 외 버튼(새 대국, 무르기, 다시 시도 등)은
  중립 톤을 유지해 accent가 실제로 눈에 띄도록 한다.

## 버튼

- 기본 버튼: `--surface-alt` 배경 + `--border` 테두리, hover 시 `--primary` 테두리로 강조
- 핵심 CTA (`.btn-accent`): `--accent` 배경 + 흰 글자
